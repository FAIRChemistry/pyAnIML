# Basic data types and mapping
# TODO extend to all parameter types

parameter_types = {
    int: "I",
    float: "F",
    str: "S",
    bool: "Boolean"
}

# Dependency enum
dependencies = [
    "dependent",
    "independent"
]
